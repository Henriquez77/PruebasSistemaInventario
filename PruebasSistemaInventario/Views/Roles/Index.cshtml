@model IEnumerable<PruebasSistemaInventario.Models.Role>

@{
    ViewData["Title"] = "Listado de roles";
}
<div class="container">
<h1>Listado de roles</h1>

<p>
        <a asp-action="Create" class="btn btn-success">Crear nuevo</a>
</p>
    <table class="table table-striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                        <a asp-action="Edit" asp-route-id="@item.RolId" class="btn btn-primary">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.RolId" class="btn btn-warning">Detalles</a> |
                        <a asp-action="Delete" asp-route-id="@item.RolId" class="btn btn-danger">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
</div>